name: Random Reviewer with Discord
description: randomly assigns reviewers to PRs and alert using Discord.
author: JedBeom

# Add your action's branding here. This will appear on the GitHub Marketplace.
branding:
  icon: user-check
  color: green

# Define your inputs here.
inputs:
  candidates:
    description: Candidates of reviewers.
    required: true
  webhook_url:
    description: The url of discord Webhook.
    required: true
  template:
    description: template
    required: false

runs:
  using: node20
  main: dist/index.js
